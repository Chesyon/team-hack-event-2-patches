#include <pmdsky.h>
#include <cot.h>

// These variables are read and defined in text_speed.c's GetTextSpeed, and assigned in uppercase_text_tags.c's HandleUppercaseVTag.
extern uint16_t text_mult;
extern uint16_t text_frac;
extern uint16_t text_count;